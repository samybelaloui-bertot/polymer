export class VolvoLogEvent extends CustomEvent {
  constructor(init) {
    if (init.detail == null) {
      init.detail = {
        level: 'info'
      };
    }

    if (init.detail != null && init.detail.timestamp == null) {
      init.detail.timestamp = Math.round(Date.now() / 1000).toString();
    }

    super(VolvoLogEvent.eventName, init);
  }

}
VolvoLogEvent.eventName = 'volvo-log'; //# sourceMappingURL=volvo-log-event.js.map